include(BundleUtilities)
message("FOO ${TARGET_FILE_PATH}")
if(TARGET_FILE_PATH MATCHES exe$)
    fixup_bundle("${TARGET_FILE_PATH}" "" "@RUNTIME_DIRS@")
elseif(TARGET_FILE_PATH MATCHES lib$)
    fixup_bundle("" "${TARGET_FILE_PATH}" "@RUNTIME_DIRS@")
endif()
